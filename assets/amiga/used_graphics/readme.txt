this directory contains binary log files computed by playing the game and logging
the used tiles/sprites with their cluts/modes.

Generating the whole tileset/spriteset is not an option here, too many colors, as
some cluts are used by status and some by main screen
