- move tile refresh code in IRQ to speed main code (flag refresh instead of redrawing)
- highscore load/save
- GAME OVER is in black should be white
- export stack symbol to reset it at A7A
- mainloop is called roughly 10 times as much as irq
  * on fast machines, some timers have issues (car lockup)
  * on slow machines, game doesn't run, as irq triggers too often
  regulate / call irq less often?
  
- after game over no credits, explanation screen is buggy
  8FE7... attributes for instructions is completely shifted
  bonus flag list is also printed too low/too high
- cycle exact adf not working / completely broken is this the same
  issue as digdug2?
- engine sfx: make frequency vary depending on orientation
- freeze code / manually profile & optimize
- sprite color issue when cars are on the bottom

- cheat add to score: > 20000 doesn't work
  we should call the "addtoscore" routine
- add unused tune to "game over" screen: doesn't work, music stop called?
- sometimes starts with 99 credits (ex: from floppy, starts immediately with 99 credits)
  
- kind of level skip flag picked always complete level trainer
- sometimes cars are all red: snap sprites at this moment for mockup

- infinite fuel: not when level ends / skip fuel bonus
- level skip




complete level on first flag: bpset 18CB,,{PC=$18CE;g}
skip routine: bpset 0DB2,,{PC=$184;g}

stop engine sound & in-game tunes: 
bpset 27b3,,{PC=$184;g}
bpset 251c,,{PC=$184;g}




after maze drawn: 08FC